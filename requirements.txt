# Airflow dependencies
apache-airflow[postgres]==2.5.0  # Adjust version as needed
psycopg2-binary==2.9.6           # PostgreSQL driver for Airflow

# Other common dependencies (add as needed)
pandas==1.5.3                     # For data manipulation
requests==2.28.1                  # For making HTTP requests
